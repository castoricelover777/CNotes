from __future__ import annotations

import datetime as dt
import os
import shutil
import sqlite3
from pathlib import Path


TOPIC = "1.1 数据结构的研究内容"

SUMMARY = """本集主线：数据结构研究的不是孤立的数据，而是计算机操作对象、对象之间的关系，以及施加在这些对象上的操作。

计算机解决问题的一般过程：
1. 从现实问题中抽象出数学模型；
2. 针对模型设计算法；
3. 用程序设计语言实现、调试并运行。

本集通过四类问题引出三种典型结构：
- 信息管理系统 → 有前后次序的线性表；
- 井字棋的派生棋局 → 树；
- 磁盘目录 → 树；
- 地图导航 → 图。
"""

CODE = r'''#include <stdio.h>

/* 1. 线性表：学生记录按顺序排列 */
typedef struct {
    int id;
    char name[20];
    int score;
} Student;

/* 2. 树：一个棋局可以派生出多个下一步棋局 */
typedef struct GameState {
    int board[3][3];
    struct GameState *children[9];
    int child_count;
} GameState;

/* 3. 图：地点是顶点，道路是边，权值可以表示距离或时间 */
#define CITY_COUNT 4

typedef struct {
    const char *name[CITY_COUNT];
    int distance[CITY_COUNT][CITY_COUNT];
} MapGraph;

int main(void)
{
    Student students[] = {
        {1001, "Li", 90},
        {1002, "Wang", 86},
        {1003, "Zhao", 95}
    };

    MapGraph map = {
        {"学校", "地铁站", "商场", "广场"},
        {
            {0, 5, 12, 20},
            {5, 0, 6, 10},
            {12, 6, 0, 4},
            {20, 10, 4, 0}
        }
    };

    printf("线性表第一条记录：%s，%d分\n",
           students[0].name, students[0].score);
    printf("从%s到%s的直接距离：%d\n",
           map.name[0], map.name[3], map.distance[0][3]);

    return 0;
}
'''

CODEX_SUMMARY = """【课程定位】
视频第 2 集《第01周b—1.1 数据结构研究》回答的是一个根本问题：为什么学习数据结构，以及数据结构究竟研究什么。

这一集暂时没有急着讲数组、链表或具体算法，而是先建立一套观察问题的方法：面对一个现实问题，先找出计算机真正要操作的对象，再找出这些对象之间的关系，最后明确要对它们执行哪些操作。

【一、计算机解决问题的完整过程】
老师把过程分成三步：

1. 抽象数学模型
从现实问题中排除无关细节，只保留计算机能够表示和操作的对象，以及对象之间的关系。

2. 设计算法
数学模型确定以后，再设计解决问题的步骤。算法回答“怎样一步一步得到结果”。

3. 编程、调试与运行
选择一种程序设计语言，把算法写成程序，上机调试，直到程序能够正确运行。

可以把这三步记成：
现实问题 → 数据模型 → 算法 → 程序

许多初学者一看到题目就马上写代码，容易卡住。真正应该先问的是：
- 我要处理的对象是什么？
- 对象之间有什么关系？
- 我要执行哪些操作？

【二、什么叫“抽象出数学模型”】
抽象并不是把问题变得更复杂，而是删去现实世界中暂时不重要的细节，只留下计算机需要处理的部分。

具体过程是：
1. 分析现实问题；
2. 找出计算机可以操作的对象；
3. 分析对象之间的关系；
4. 用合适的数学或逻辑形式描述对象和关系。

对象加上对象之间的关系，就形成了这一集所强调的数据结构。

【三、早期重点：数值计算】
早期计算机主要用于数值计算，例如求解工程中的线性方程组、预测人口增长等。

这类问题通常具有两个特点：
- 操作对象之间的关系比较容易用方程描述；
- 真正困难的是计算量很大。

例如矩阵和向量可以用数组保存，之后由计算机快速完成大量运算。此时研究重点更多是数值计算方法。

【四、现代重点：非数值计算】
随着计算机应用范围扩大，大量问题不再以“求一个数”为目标，而是处理各种信息。

例如学生信息系统中会保存：
- 学号、姓名、性别、籍贯和专业；
- 课程名称、成绩等记录。

对这些信息最常见的操作不是加减乘除，而是：
- 增：加入一条新记录；
- 删：删除离校或无效记录；
- 查：按学号或姓名查找；
- 改：修改姓名、成绩或其他字段。

这就是常说的“增删查改”。非数值计算的关键不再是复杂算式，而是如何组织对象，使这些操作更清楚、更高效。

【五、例子一：信息管理系统为什么是线性表】
学生、图书、员工、仓库物品和联系人等记录，通常一条接一条地排列。

这种关系具有明显的前后次序：
- 第一条记录没有前驱；
- 最后一条记录没有后继；
- 中间记录通常各有一个前驱和一个后继。

这种一对一的前后关系叫线性关系，由此抽象出的结构就是线性表。

线性表关心的不是某一条记录具体叫李明还是王芳，而是记录如何排列，以及怎样插入、删除、查找和修改。

【六、例子二：井字棋为什么形成树】
在井字棋中，一个当前棋局可能有多个空位置，因此下一步可能产生多个不同棋局，这些叫派生棋局。

选择其中一个派生棋局以后，它又能继续派生出下一层的多个棋局。于是：
- 当前棋局像根；
- 每一种下一步像一个分支；
- 分支还可以继续产生分支。

这种“一对多、逐层派生”的关系像一棵倒置的树，是典型的非线性结构。

在这个模型里：
- 操作对象是每一个棋局状态；
- 对象关系是“由哪个棋局派生而来”；
- 主要操作是按照决策策略选择下一个棋局。

【七、例子三：磁盘目录为什么也是树】
磁盘中一个目录可以包含多个子目录和文件；每个子目录通常只属于一个上级目录。

它同样是一对多的层次关系：
- 磁盘或根目录是根；
- 文件夹是中间节点；
- 文件通常是末端节点。

井字棋与目录系统的现实含义完全不同，但它们都能抽象为树。这正说明数据结构研究的是关系形式，而不是对象表面的名称。

【八、例子四：地图导航为什么是图】
地图导航需要把现实地图抽象成计算机可以计算的模型：
- 地点、路口或车站看成点，也叫顶点；
- 道路看成点与点之间的连线，也叫边；
- 距离、时间或费用可以作为边的权值。

地点之间可能互相连接，形成网状关系。这种多对多关系不能简单表示成线性表，也不一定具有树的上下级关系，因此使用图来表示。

导航程序随后可以在图上研究：
- 两个地点是否连通；
- 可以经过哪些路线；
- 哪条路线距离最短；
- 哪条路线耗时最少。

【九、三种结构的对比】
线性表：一对一的前后关系。
例子：学生名单、图书记录、联系人列表。

树：一对多的层次关系。
例子：棋局派生、磁盘目录、组织结构。

图：多对多的网状关系。
例子：道路网络、地铁网络、社交关系。

判断结构时不要只看对象是什么，而要看对象怎样发生联系。

【十、数据结构研究什么】
本集最后把研究对象落在三个方面：

1. 计算机要操作的数据对象
例如学生记录、棋局状态、文件目录、地图地点。

2. 数据对象之间的关系
例如线性关系、层次关系和网状关系。

3. 施加在这些对象上的操作
例如插入、删除、查找、修改、选择下一状态和寻找最短路径。

因此可以用一句话概括：
数据结构研究非数值计算问题中，操作对象、对象之间的逻辑关系，以及对这些对象所进行的操作。

【C 语言建模提示】
代码区给出了三个概念模型：
- Student 数组演示线性表中的顺序记录；
- GameState 的 children 演示一个棋局派生多个子棋局；
- MapGraph 的邻接矩阵演示地点之间的距离关系。

这段代码的目的不是实现完整的数据结构，而是让你看到同一个现实问题如何逐渐变成 C 语言中的结构体、数组、指针和矩阵。

【学习检查】
学完本集后，应该能回答：
1. 计算机解决现实问题通常经过哪三个阶段？
2. 抽象数学模型时必须找出哪两样东西？
3. 为什么学生信息管理适合抽象成线性表？
4. 为什么井字棋和目录系统都可以抽象成树？
5. 地图中的点、边和权值分别表示什么？
6. 数据结构最终研究哪三个方面？

【一句话记忆】
先找对象，再看关系，最后确定操作；对象和关系的组织方式，就是数据结构研究的核心。
"""


def main() -> None:
    app_dir = Path(os.environ.get("APPDATA", Path.home())) / "CNotes"
    database = app_dir / "notes.db"
    backup_dir = app_dir / "backups"
    backup_dir.mkdir(parents=True, exist_ok=True)
    stamp = dt.datetime.now().strftime("%Y%m%d-%H%M%S")
    backup = backup_dir / f"notes-before-data1-note1-{stamp}.db"
    shutil.copy2(database, backup)

    data1 = "数据1"
    title = "数据1之1"
    now = dt.datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    connection = sqlite3.connect(database)
    try:
        with connection:
            cursor = connection.execute(
                """
                UPDATE notes
                SET topics = ?, summary = ?, codex_summary = ?, code = ?, updated_at = ?
                WHERE note_date = ? AND title = ?
                """,
                (TOPIC, SUMMARY, CODEX_SUMMARY, CODE, now, data1, title),
            )
        if cursor.rowcount != 1:
            raise RuntimeError(f"Expected one note, updated {cursor.rowcount}")
        row = connection.execute(
            "SELECT LENGTH(summary), LENGTH(codex_summary), LENGTH(code) FROM notes WHERE note_date=? AND title=?",
            (data1, title),
        ).fetchone()
        print(f"updated={cursor.rowcount} lengths={row} backup={backup}")
    finally:
        connection.close()


if __name__ == "__main__":
    main()
