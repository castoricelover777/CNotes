import os
import shutil
import sqlite3
from datetime import datetime


DB_PATH = os.path.expandvars(r"%APPDATA%\CNotes\notes.db")

SUMMARIES = {
    48: """【总结】数组用于把一组“类型相同、用途相近”的数据放在一块连续的存储空间里，例如一组成绩、商品价格、温度或计数结果。数组下标从 0 开始，长度为 n 的数组最后一个元素是 a[n-1]。学习数组时要把“数据本身”和“下标范围”一起考虑：下标不是从 1 开始，循环条件通常写成 i < n，而不是 i <= n。\n【用法】常见模板是 int score[5];，再用 for (int i = 0; i < 5; i++) 依次读写 score[i]。遍历求和、求最大值、统计满足条件的元素，本质都是“初始化结果变量 + 循环访问每个元素 + 更新结果”。数组适合保存批量数据，但长度固定，使用前应明确容量；如果输入数量未知，应结合动态内存。\n【易错点】越界访问不会自动报错，却可能覆盖别的变量；不要访问 a[5]（当数组长度为 5 时）。数组元素必须是同一种类型，不能把 int 数组直接当成 char 数组使用。""",
    49: """【总结】数组定义同时决定元素类型、数组名和容量。int a[5]; 表示有 5 个 int 元素，合法下标为 0～4。定义时可以用初始化列表，例如 int a[] = {2, 4, 6};，编译器会根据元素个数推断长度；也可以写 int a[5] = {1, 2};，未写出的元素会被初始化为 0。\n【用法】数组经常在定义后配合循环使用：先定义容量，再用 i < sizeof(a) / sizeof(a[0]) 计算元素个数，避免手写长度出错。需要清零时可写 int a[5] = {0};。函数中传入数组时，通常同时传入长度，例如 void print(int a[], int n)，函数内部按 0 到 n-1 访问。\n【易错点】数组一旦定义，容量通常不能靠 a = another_array 重新赋值；a[0] = ... 是修改元素。不要把“数组容量”和“当前有效元素个数”混为一谈，部分填充的数组必须另存有效长度。""",
    50: """【总结】sizeof 求的是对象占用的字节数，不是元素个数。对完整数组 a 使用 sizeof(a) 得到整个数组大小，sizeof(a[0]) 得到单个元素大小，因此计算元素个数的通用写法是 sizeof(a) / sizeof(a[0])。这种写法会随元素类型和数组长度变化自动适配，适合本地数组遍历。\n【用法】例如 int a[] = {3, 5, 7, 9};，int n = sizeof(a) / sizeof(a[0]);，随后 for (int i = 0; i < n; i++) 访问 a[i]。sizeof 的结果类型是 size_t，若用 %zu 打印；与 int 混用时要注意有符号/无符号比较。把数组传给函数后，数组名会退化为指针，sizeof(a) 不再代表整个数组，所以必须把 n 作为参数传入。\n【易错点】不能在函数参数 int a[] 中再次用 sizeof(a) 求数组长度；不能把 sizeof 得到的字节数直接当作元素个数。对指针 p 使用 sizeof(p) 得到的是指针大小，而不是它指向的数组大小。""",
    51: """【总结】素数判断的核心是：大于 1 的整数 n，如果除了 1 和自身外没有其他因数，就是素数。最直接的算法从 2 试除到 n-1；更实用的做法只检查 i*i <= n，因为如果 n 有一个大于平方根的因数，必然同时存在一个不大于平方根的因数。\n【用法】可用 int is_prime(int n) 函数封装：n < 2 直接返回 0；设置 flag = 1，循环 for (int i = 2; i * i <= n; i++)，若 n % i == 0 就把 flag 置 0 并 break。主函数再遍历区间，把返回 1 的数打印出来。break 能在找到因数后提前结束，减少无用计算；continue 适合跳过不符合条件的输入。\n【易错点】0 和 1 都不是素数；不要把“不能被 2 整除”误判成素数。i*i 在极大整数上可能溢出，严谨写法可改为 i <= n / i。判断函数应明确返回值含义，避免把 flag 未初始化或循环边界写错。""",
    52: """【总结】二维数组可以理解为“数组中的每个元素仍然是一维数组”，适合表示表格、矩阵、棋盘和班级成绩表。int matrix[3][4] 表示 3 行 4 列，访问方式是 matrix[row][col]，两个下标都从 0 开始。内存中通常按行连续存放，因此遍历一般使用外层行循环、内层列循环。\n【用法】典型模板是 for (int i = 0; i < rows; i++) for (int j = 0; j < cols; j++) matrix[i][j] = ...。打印、求每行和、矩阵相加都采用嵌套循环。函数传二维数组时，第一维可以省略，但第二维必须让编译器知道每行跨度，例如 void print(int a[][4], int rows)。如果行列都运行时才确定，可使用动态内存或变长数组。\n【易错点】matrix[i][j] 中 i 是行、j 是列，不能越界；把列数写错会导致地址计算错误。二维数组和 int ** 不是同一种内存布局，不能随意互换。""",
    56: """【总结】本课代码用变量 number 演示取地址运算符：int number = 42; 创建一个整型变量，&number 得到它在内存中的地址。地址不是普通整数，打印地址应使用 %p，并转换为 (void *)，因此代码中的 printf(\"address = %p\\n\", (void *)&number); 是规范写法。\n【代码理解】第一条 printf 读取 number 的值；第二条 printf 读取 number 的地址。& 的结果可以交给指针保存，也可以作为函数参数传出结果。地址会因运行环境、每次运行甚至编译方式而变化，所以不能把某个地址当成固定常量。\n【用法】当函数需要修改调用者的变量、返回多个结果，或需要操作数组/动态内存时，就要传地址：void set_value(int *p) { *p = 100; }，调用 set_value(&number)。取地址和解引用是一对相反操作：&number 得地址，*p 根据地址取回变量。\n【易错点】不要用 %d 打印地址；不要对未初始化指针直接解引用；& 只能用于可寻址对象，不能写 &42。""",
    57: """【总结】本课代码完整展示“指针保存地址并通过地址修改变量”。int number = 10; 先建立变量；int *pointer = &number; 声明一个指向 int 的指针并让它指向 number；printf 中用 (void *)pointer 配合 %p 输出地址；最后 *pointer = 20; 通过解引用把 number 改为 20。\n【代码理解】pointer 保存的不是 10，而是 number 的地址；*pointer 才代表地址处的那个 int 对象。因此修改 *pointer 等价于修改 number。指针类型必须与目标对象匹配，int * 应指向 int，编译器才能正确计算读写所需的字节数。\n【用法】指针常用于函数修改外部变量、遍历数组和访问结构体。使用前先让指针指向有效对象，或初始化为 NULL；使用时写 if (pointer != NULL) 再解引用。若只想读取地址处的值用 *pointer，若想改变值就给 *pointer 赋值。\n【易错点】pointer 本身和 *pointer 是两个不同概念；未初始化的野指针、指向已释放内存的悬空指针都不能解引用。""",
    58: """【总结】本课的 swap 代码说明了指针最重要的应用：让函数真正修改调用者的两个变量。void swap(int *a, int *b) 接收两个地址，temp 暂存 *a，随后 *a = *b、*b = temp 完成交换。main 中写 swap(&x, &y)，传入 x、y 的地址，因此函数操作的是原变量，而不是副本。\n【代码理解】如果参数写成 void swap(int a, int b)，函数收到的是值的拷贝，交换只发生在函数内部；改成 int *a、int *b 后，*a 和 *b 分别代表调用者的 x、y。temp 必不可少，否则先写入 *a 会丢失原来的值。\n【用法】凡是函数需要修改外部变量、输出多个结果，都可以使用指针参数，例如 void minmax(int a[], int n, int *min, int *max)。调用时传入 &min、&max，函数内通过 *min、*max 写回结果。也可以用指针遍历数组，但必须保证指针指向有效元素。\n【易错点】调用 swap 时不能写 swap(x, y)；函数内不能忘记解引用，否则只会修改指针副本。传入 NULL 或同一地址时要根据需求处理。""",
    59: """【总结】数组和指针关系紧密，但不是同一个东西。大多数表达式中，数组名 a 会退化为指向首元素的指针 &a[0]；所以 a[i] 与 *(a + i) 等价，编译器把下标访问转换为“首地址加偏移再解引用”。数组占一整块连续空间，指针只是保存地址的变量。\n【用法】函数处理数组时常写 void print(const int *a, int n) 或 void print(int a[], int n)，两种参数形式在函数中都按指针处理。传入数组首地址和长度，循环访问 a[i]。若函数不修改元素，加 const 保护数据。需要遍历时可用 for (int *p = a; p < a + n; p++)。\n【易错点】数组传入函数后不能靠 sizeof(a) 求长度；a 与 &a 的类型和步长不同，虽然地址数值常相同。数组名不能自增，p++ 才是移动指针。""",
    60: """【总结】const 与指针组合时，关键是看 const 修饰“指向的数据”还是“指针本身”。const int *p（也可写 int const *p）表示不能通过 p 修改 *p，但 p 可以改指向；int *const p 表示 p 的指向固定，但可以修改 *p；const int *const p 则两者都不能改。\n【用法】只读数组参数应写 const int *a，函数内部可以读取 a[i]，却不能误改原数组。需要固定指针指向某个对象时使用 int *const p，例如一个函数内部的别名。接口设计中优先给“只读数据”加 const，编译器会帮助发现误修改。\n【易错点】const 不是让对象绝对不可变，而是禁止通过该表达式修改；原对象若本来不是 const，仍可能通过其他合法路径修改。不要用强制类型转换去掉真正 const 对象的只读属性，这可能产生未定义行为。""",
    61: """【总结】指针运算不是按 1 个字节移动，而是按所指向类型的大小移动。对 int *p 执行 p + 1，地址会前进 sizeof(int) 个字节；p++ 指向下一个 int 元素。指向同一数组的两个指针可以相减得到元素间距离，也可以比较先后位置。\n【用法】数组遍历可写成 int *p = a; while (p < a + n) { printf(\"%d\", *p); p++; }。p + n 指向数组末尾后一个位置，只能用于比较，不能解引用。指针相减结果类型为 ptrdiff_t；下标 a[i] 本质上就是 *(a + i)，选择哪种写法取决于可读性。\n【易错点】只有同一数组（或末尾后一位）内的指针相减/比较才有定义；不能对任意两个地址做减法。指针越过有效范围后不能解引用，p++ 也不能替代边界检查。""",
    64: """【总结】动态内存分配用于运行时才知道数据规模的场景。malloc(n * sizeof *p) 在堆上申请 n 个元素的空间，返回 void *，在 C 中可直接赋给目标指针；使用完必须 free(p)。申请失败时 malloc 返回 NULL，不能继续解引用。\n【用法】典型流程是：计算字节数→malloc→检查 NULL→写入/读取→free。比如 int *a = malloc(n * sizeof *a);，申请后用 a[i] 访问，再 free(a)。需要调整空间可用 realloc，但要先用临时指针接收返回值，避免失败时丢失原指针。calloc 会申请并清零。\n【易错点】少乘 sizeof 会造成空间不足；free 后不能再访问（悬空指针），也不能重复 free。内存泄漏通常来自申请后忘记释放或覆盖唯一指针。不要把栈数组、字符串字面量传给 free。""",
    65: """【总结】C 字符串不是独立类型，而是以 '\\0' 结尾的 char 数组。字符串长度不包含 '\\0'，但存储空间必须额外留一个字节，例如存放 5 个可见字符至少需要 char s[6]。因此处理字符串时既要关注内容，也要关注容量和终止符。\n【用法】定义可修改字符串可写 char s[] = \"hello\";，读取字符用 s[i]，输出用 %s。常见库函数 strlen、strcmp、strcpy、strcat 都要求参数指向以 '\\0' 结尾的有效字符串。复制或拼接前要预留足够空间，并把目标数组声明为可写。\n【易错点】char *p = \"hello\"; 指向字符串字面量，不能通过 p[0] 修改；strlen 的结果不是包含终止符的数组容量；忘记 '\\0' 会导致 printf/strlen 越过数组继续读内存。""",
    66: """【总结】字符串变量和字符串常量的区别决定了能不能修改。char s[] = \"abc\"; 会创建一个可修改的字符数组，并把 a、b、c、'\\0' 复制进去；const char *p = \"abc\"; 通常指向只读字符串字面量，不能写 p[0]。两者都能用于 printf(\"%s\", ...)，但存储方式和生命周期不同。\n【用法】需要修改内容、接收输入或调用 strcpy 目标参数时使用 char 数组，并预留容量，如 char name[32] = \"Tom\";。只读固定文本可用 const char *，适合作为函数参数：void print(const char *s)。多个字符串常量可用 const char *words[] 保存指针数组。\n【易错点】数组名不是可重新赋值的指针，不能写 s = \"new\"；可以用 strcpy(s, \"new\")，前提是容量足够。任何来自外部输入的字符串都必须检查长度，不能假设一定短。""",
    68: """【总结】字符串数组有两种常见形态：二维字符数组 char words[3][20]，每行拥有固定 20 字节、内容可修改；指针数组 const char *words[]，每个元素保存一个字符串地址，长度不必相同但字面量应视为只读。选择哪一种取决于是否需要修改字符串和是否希望节省空间。\n【用法】二维数组适合批量接收输入：fgets(words[i], sizeof words[i], stdin)。指针数组适合菜单、命令表、月份名等固定文本，用 for 遍历 words[i] 输出。若要修改指针数组指向的内容，必须让它指向可写数组，不能直接修改字符串字面量。\n【易错点】sizeof(words) / sizeof(words[0]) 只有在当前作用域仍是完整数组时才可求数量；传入函数后要额外传长度。二维数组和 char ** 不能随意互换，内存布局不同。""",
    67: """【总结】这段代码演示了安全读取一行字符串的基本方式。char line[100]; 申请 100 个 char 的缓冲区；printf 提示用户输入；fgets(line, sizeof(line), stdin) 从标准输入读取，最多写入 99 个字符，并自动保留结尾的换行（若读到了），最后补 '\\0'；与 NULL 比较可以判断读取是否成功；printf(\"你输入的是：%s\", line) 按字符串输出。\n【代码理解】sizeof(line) 在这里得到整个数组的字节数 100，因此传给 fgets 能避免写出边界。fgets 比 scanf(\"%s\") 更适合读取包含空格的一整行。若输入超过 99 个字符，fgets 只取前 99 个，剩余内容留在输入流中，后续读取可能读到残余字符。\n【用法】读取后若不想保留换行，可查找 line 中的 '\\n' 并替换为 '\\0'；需要继续读取时应处理超长输入。函数中传入字符数组时同时传入容量，始终让 fgets 使用真实容量。\n【易错点】不能把 sizeof(line) 换成 sizeof(pointer)；不能对 fgets 返回值只判断字符串是否为空。使用 scanf 后紧接 fgets 时要先处理输入流中残留的换行。""",
    72: """【总结】程序参数让程序从命令行接收数据。main 可以写成 int main(int argc, char *argv[])：argc 是参数个数，至少为 1；argv 是字符串指针数组，argv[0] 通常是程序名，用户输入的第一个参数在 argv[1]。所有参数最初都是字符串，需要用 atoi 或更稳妥的 strtol 转成数字。\n【用法】先判断 argc 是否满足要求，再访问对应 argv[i]，避免越界。例如 if (argc < 3) { printf(\"用法: %s a b\\n\", argv[0]); return 1; }。参数可用于文件名、模式、数字范围等，让同一程序在不同输入下运行。处理数字时应检查转换是否成功、是否溢出。\n【易错点】argv[argc] 保证为 NULL，但 argv[argc-1] 才是最后一个真实参数；不能默认 argv[1] 一定存在。argv 中的内容是字符指针，不能直接用 %d 当整数打印；需要先转换。""",
    73: """【总结】strlen 用来计算 C 字符串的长度，遇到第一个 '\\0' 停止，返回值不包含终止符。它接收 const char *，所以既可处理字符数组也可处理字符串字面量。返回类型是 size_t，打印时使用 %zu。\n【用法】strlen(s) 可用于计算实际输入长度、判断是否为空、决定复制范围或拼接前剩余空间。常见条件是 if (strlen(s) == 0)，但若循环中反复调用会每次从头扫描，效率较低；可以先保存 size_t len = strlen(s)。做容量检查时要把 '\\0' 的一个字节算进去，例如目标容量至少需要 strlen(src) + 1。\n【易错点】strlen 需要合法的 '\\0' 结尾，未终止的 char 数组会越界读取；不能用 sizeof(pointer) 代替 strlen。strlen 返回 size_t，与负数比较时要留意无符号类型。""",
    74: """【总结】strcmp 按字典序比较两个以 '\\0' 结尾的字符串，返回 0 表示相等，小于 0 表示第一个字符串较小，大于 0 表示第一个较大。不要把返回值理解为只能是 -1、0、1，实际数值由实现决定，判断时只看与 0 的关系。\n【用法】字符串相等写 if (strcmp(a, b) == 0)，a 是否排在 b 前写 if (strcmp(a, b) < 0)。它比较的是内容，不是指针地址，因此不能用 a == b 判断两个字符串是否相同。可用 strcmp 实现菜单命令判断、排序比较和输入校验。\n【易错点】参数必须是有效字符串；忘记 '\\0' 会导致越界读取。strcmp 区分大小写，\"A\" 与 \"a\" 不相等。不能写 if (strcmp(a,b)) 来表示相等，因为非零恰恰表示不相等。""",
    75: """【总结】strcpy 复制字符串，strcat 把一个字符串追加到目标字符串末尾。两者都以 '\\0' 为边界：strcpy 会连同终止符一起复制；strcat 会先找到目标串末尾，再复制源串和新的终止符。因此目标数组必须足够大，且目标不能是字符串字面量。\n【用法】char dest[32]; strcpy(dest, \"C\"); strcat(dest, \" language\"); 得到可修改的拼接结果。更安全的接口设计是把目标容量作为参数传入，或使用 snprintf 进行格式化拼接。复制前应确认 strlen(src) + 1 <= sizeof(dest)，追加前应确认已有长度 + 新长度 + 1 不超过容量。\n【易错点】strcpy(dest, src) 不会自动检查 dest 容量，可能造成缓冲区溢出；strcat 的 dest 必须已经是合法的 '\\0' 结尾字符串。源和目标重叠时不要使用 strcpy，应考虑 memmove；不要写 dest = src，因为数组不能直接赋值。""",
}


def main():
    backup = DB_PATH + ".bak-rewrite-day7-" + datetime.now().strftime("%Y%m%d-%H%M%S")
    shutil.copy2(DB_PATH, backup)
    conn = sqlite3.connect(DB_PATH)
    try:
        ids = sorted(SUMMARIES)
        placeholders = ",".join("?" for _ in ids)
        rows = conn.execute(f"SELECT id, note_date FROM notes WHERE id IN ({placeholders})", ids).fetchall()
        found = {row[0] for row in rows}
        missing = sorted(set(ids) - found)
        if missing:
            raise RuntimeError(f"Missing note ids: {missing}")
        for note_id, text in SUMMARIES.items():
            conn.execute(
                "UPDATE notes SET codex_summary=?, updated_at=datetime('now','localtime') WHERE id=?",
                (text, note_id),
            )
        conn.commit()
    finally:
        conn.close()
    print(f"updated={len(SUMMARIES)} backup={backup}")


if __name__ == "__main__":
    main()
